extends ../layouts/common.pug

block content
	.container(style="background-color: white; height: 100%")

		if errors
			each error in errors
				.alert.alert-danger= error

		form.m-auto(action='/admin/login', method='POST', style="width: 50%")
			h2 REGISTER
			.form-group
				label(for='exampleInputEmail1') name
				input#exampleInputEmail1.form-control(type='text', aria-describedby='emailHelp', placeholder='Enter name', name='name', value=values ? values.name: '')
			.form-group
				label(for='exampleInputPassword1') Password
				input#exampleInputPassword1.form-control(type='password', placeholder='Password', name='password', value=values ? values.password : '')

			button.btn.btn-primary(type='submit') Submit	